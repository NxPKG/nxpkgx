{
  "private": true,
  "scripts": {
    "build": "nxpkg run build",
    "clean": "nxpkg run clean",
    "dev": "nxpkg run dev --no-cache --continue",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "lint": "nxpkg run lint",
    "test": "nxpkg run test"
  },
  "devDependencies": {
    "eslint": "^8.4.1",
    "prettier": "^2.5.1",
    "tsconfig": "workspace:*",
    "nxpkg": "latest"
  },
  "packageManager": "pnpm@8.9.0"
}
